# Contact-form endpoint. Currently the still-live Django API on App Runner.
# Switch to the Cloudflare Worker (https://contact.encodestudio.in) once it is
# deployed with its Resend secret and DNS route — see /worker/README.md.
VITE_CONTACT_ENDPOINT=https://api.encodestudio.in/api/contact/
